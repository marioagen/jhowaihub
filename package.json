{
    "name": "woopiai-hub",
    "version": "1.0.0",
    "private": true,
    "description": "WoopiAI Hub - Root package for formatting and linting",
    "scripts": {
        "format": "prettier --write \"**/*.{json,yml,yaml,md}\" --ignore-path .prettierignore",
        "format:check": "prettier --check \"**/*.{json,yml,yaml,md}\" --ignore-path .prettierignore",
        "format:frontend": "cd front-end/vueapp && npm run format",
        "format:backend": "dotnet format WoopiaiHub.sln --include-generated",
        "format:backend:check": "dotnet format WoopiaiHub.sln --verify-no-changes --include-generated",
        "format:all": "npm run format && npm run format:frontend && npm run format:backend",
        "lint:frontend": "cd front-end/vueapp && npm run lint"
    },
    "devDependencies": {
        "prettier": "^3.6.2"
    },
    "engines": {
        "node": ">=14.0.0"
    }
}
